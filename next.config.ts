import type { NextConfig } from "next";

const nextConfig: NextConfig = {
   output: "export",
  // basePath: "/dbcd20.github.io",
  // assetPrefix: "/dbcd20.github.io/",
  // images: {
  //   unoptimized: true,
  // }
};

export default nextConfig;
